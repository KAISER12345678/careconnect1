# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/careconnect?schema=public"

# Auth
AUTH_JWT_SECRET="change_me_to_a_long_random_string"
APP_BASE_URL="http://localhost:3000"

# App config
APP_TZ_OFFSET_MINUTES=60
DEFAULT_CURRENCY="MAD"

# Email (optional for reminders)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="CareConnect <no-reply@careconnect.local>"
